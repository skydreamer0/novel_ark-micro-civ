---
description: 從讀取上一章到寫完正文到更新追蹤表，包含正文撰寫的核心規則（模式B）。
---

# 端到端寫章節 (Write Chapter)

此工作流為 **【模式B：正文生成】**，用於根據已確認的大綱寫出章節正文。確保不隨意發揮，在既定世界觀與既定主線下穩定產出。

## 核心寫作原則
1. **每章都必須推進**，不能原地踏步，至少推進戰力、文明、真相三條主線中的兩條。
2. 開頭 500 字內必須出現危機、任務、交易、追殺、衝突或異常。
3. 每章至少有一個**不可逆變化**，升級/收穫都必須付出**代價**。
4. 世界觀說明不能百科化，必須附著在事件、對話或戰鬥中。
5. 敵方不准降智，不可為了爽點製造低級失誤。
6. 不可將內部文明寫成脫節支線，文明線必須能反哺外部。
7. 整體基調：壓迫感、危機感、算計感、金屬感、殘酷生存感。

## 執行步驟

### Phase 1: 脈絡對齊（自動執行）
// turbo
1. 讀取上一章完整內容（`01_NOVEL_CONTENT/0X_第X卷/` 中最新章節），重點關注最後 500 字以保持連續性。
// turbo
2. 讀取 `02_PROJECT_DATABASE/STATE.md (§1+§3+§9)`（分卷大綱與進度錨點）。
// turbo
3. 讀取 `02_PROJECT_DATABASE/STATE.md §6 開放伏筆`（伏筆追蹤表）。
// turbo
4. 讀取 `02_PROJECT_DATABASE/CANON_power.md §1-§9 主機版本`（力量與文明系統）。
// turbo
5. 讀取 `.ai/視角技法示範.md`（視角鎖定與感知寫作範例）。

### Phase 2: 連貫性與專家護欄
進行【模式B】撰寫前，依據 `02_PROJECT_DATABASE/RULES.md §7 專家小組` 的六位專家同步監督：
- **不否定既有動機**：依據角色利益導向行事。
- **禁止打破第四壁**：完全沉浸，無後設視角。
- **消除拼接感**：統一敘事口吻。

### Phase 3: 【模式B】正文撰寫與標準輸出格式
嚴格依據大綱撰寫正文：
- 使用繁體中文。
- 字數以 **3000 到 4500 字** 為目標（除非另有要求）。
- 戰鬥必須改變局勢，對話必須服務於信息差與壓力。
- 章尾必須留下可直接承接下一章的壓力。

**正文寫入** `01_NOVEL_CONTENT/0X_第X卷/第XXX章_章名.md`。

**正文產出後，必須在文末以 HTML 註釋格式附上（嚴禁直接暴露在正文中）：**
```markdown
<!--
1. 本章推進了哪些主線（說明具體內容）
2. 哪個伏筆被觸發、推進或回收
3. 哪個變化不可逆
4. 下章可直接承接的壓力
-->
```


### Phase 4: 文檔同步
// turbo
1. 掃描本章正文未收錄專有名詞，提示使用者更新 `02_PROJECT_DATABASE/CANON_world.md §23 統一術語表`。
// turbo
2. 標記 `02_PROJECT_DATABASE/STATE.md §6 開放伏筆` 中需要更新的伏筆狀態。
// turbo
3. 更新 `02_PROJECT_DATABASE/STATE.md §7 衝突弧線` 中相關弧線的張力狀態。
